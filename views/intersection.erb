<p>Here is an example of an intersection table with data, fetched with the command <pre><code><%= @intersectionQ %></code></pre>.
<table class="table table-striped table-responsive">
  <tr>
  <%@tableHead.each do |headerData| %>
    <th><%= headerData %></th>
  <% end %>
</tr>
<%@tableGetter.each do |row| %>
<tr>
  <td><%= row["Name"] %></td>
</tr>
<%end%>
</table>
<form action='/' method='get'>
<button type='submit' class='btn btn-primary'>Return</button>
</form> </body>
